--use dcoda_acme;

CREATE TABLE session (
        appl varchar(20),
        sessionid varchar(25) PRIMARY KEY,  
        userid char(25),
        username char(35),
        DATE_TS datetime,
        UPDATE_TS datetime,
        row_count int,
        sort varchar(25),
        sessiondata varchar(8000)
);
